name: Deploy Worker Service

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

env:
  JOB_NAME: qwintly-builder
  IMAGE_NAME: qwintly-builder

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_QWINTLY_GIT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ vars.GCP_PROJECT_ID_QWINTLY }}

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ vars.REGION }}-docker.pkg.dev

      - name: Build Docker Image
        run: |
          IMAGE=${{ vars.REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID_QWINTLY }}/qwintly-images/${{ env.IMAGE_NAME }}:${{ github.sha }}

          gcloud builds submit \
            --tag $IMAGE \
            .

          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy $JOB_NAME \
            --image=$IMAGE \
            --region=${{ vars.REGION }} \
            --platform=managed \
            --allow-unauthenticated \
            --cpu=1 \
            --service-account ${{ vars.BUILDER_SA_EMAIL }} \
            --memory=512Mi \
            --concurrency=80 \
            --max-instances=3 \
            --min-instances=0 \
            --timeout=420 \
            --set-env-vars="SNAPSHOT_BUCKET=${{ vars.SNAPSHOT_BUCKET }},TEMPLATE_BUCKET=${{ vars.TEMPLATE_BUCKET }},GCP_PROJECT_ID_QWINTLY=${{ vars.GCP_PROJECT_ID_QWINTLY }}" \
            --set-secrets="NEXT_PUBLIC_SUPABASE_URL=supabase-url:latest,SUPABASE_SECRET_KEY=supabase-secret-key:latest,GEMINI_API_KEY=gemini-api-key:latest"
